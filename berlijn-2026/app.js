const DAYS=window.BERLIN_DAY_PARTS||[];const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const mapsSearch=q=>'https://www.google.com/maps/search/?'+new URLSearchParams({api:'1',query:q});const mapsDir=(a,b,m)=>'https://www.google.com/maps/dir/?'+new URLSearchParams({api:'1',origin:a,destination:b,travelmode:m});function best(l,m){const r=l.recommend.toLowerCase();return(m==='walking'&&r.includes('lopen')||m==='transit'&&r.includes('ov')||m==='driving'&&r.includes('taxi'))?'best':''}function save(l){if(l.walk<=15||l.scenic)return'';const x=Math.min(l.transit,l.taxi),d=l.walk-x;if(d<5)return'';return`<p class="saving">⏱️ ${l.taxi<=l.transit?'Taxi':'OV'} bespaart ongeveer ${d} minuten ten opzichte van lopen.</p>`}function media(s){const title=s.wiki_exact?esc(s.wiki_exact):'';return`<div class="media" data-wiki="${title}"><div class="fallback"><b>${esc(s.typeIcon)}</b><span>${s.wiki_exact?'Foto wordt geladen van de exacte Wikipedia-pagina':'Geen betrouwbare locatie­foto toegevoegd'}</span></div><img alt="${esc(s.title)}" loading="lazy"><a class="credit" target="_blank" rel="noopener">📷 Wikipedia / Wikimedia Commons</a><span class="letter">${esc(s.letter)}</span></div>`}function stop(day,s){return`<article class="stop">${media(s)}<div class="content"><div class="time">${esc(s.time)}</div><h3>${esc(s.title)}</h3><div class="badges"><span>${esc(s.typeIcon)} ${esc(s.type)}</span><span>⏳ ${esc(s.visit)}</span><span>📌 ${esc(s.inside)}</span></div><p>${esc(s.summary)}</p><details><summary>Meer uitleg en praktisch</summary><div class="details"><p><strong>Wat doen jullie?</strong> ${esc(s.details)}</p><p><strong>Praktisch:</strong> ${esc(s.practical)}</p><p><strong>Adres:</strong> ${esc(s.address)}</p></div></details><div class="buttons"><a class="primary" href="${mapsSearch(s.address)}">📍 Google Maps</a><a href="${esc(s.video)}">▶️ Filmpjes</a>${s.official?`<a href="${esc(s.official)}">🌐 Website</a>`:''}</div></div></article>`}function leg(day,l){const a=day.stops.find(x=>x.letter===l.from),b=day.stops.find(x=>x.letter===l.to);return`<section class="leg"><div class="legtop"><strong>${l.from} → ${l.to}: ${esc(b.title)}</strong><span>${esc(l.distance)}</span></div><div class="modes"><a class="mode ${best(l,'walking')}" href="${mapsDir(a.address,b.address,'walking')}">🚶<b>${l.walk} min</b><span>Lopen</span><small>live route</small></a><a class="mode ${best(l,'transit')}" href="${mapsDir(a.address,b.address,'transit')}">🚆<b>${l.transit} min</b><span>OV</span><small>live route</small></a><a class="mode ${best(l,'driving')}" href="${mapsDir(a.address,b.address,'driving')}">🚕<b>${l.taxi} min</b><span>Taxi</span><small>rijroute</small></a></div><div class="advice"><strong>Advies: ${esc(l.recommend)}</strong><br>${esc(l.note)}</div>${save(l)}</section>`}function day(d){let flow='';d.stops.forEach((s,i)=>{flow+=stop(d,s);if(d.legs[i])flow+=leg(d,d.legs[i])});const hero=d.stops.find(s=>s.wiki_exact)||d.stops[0];return`<section class="day" id="${d.id}"><div class="dayhead media" data-wiki="${esc(hero.wiki_exact||'')}"><div class="fallback"><b>🗺️</b><span>${esc(d.date)}</span></div><img alt="${esc(d.title)}"><a class="credit" target="_blank" rel="noopener">📷 Wikipedia / Wikimedia Commons</a><div class="shade"></div><div class="daytitle"><span>${esc(d.date)}</span><h2>${esc(d.title)}</h2><p>${esc(d.summary)}</p></div></div><div class="meta"><span>🌡️ ${esc(d.weather)}</span><span>🕒 ${d.start}–${d.end}</span><span>🚶 ${esc(d.stats.walk)}</span><span>📍 ${esc(d.stats.visit)}</span></div><div class="routes">${d.fullRoutes.map(r=>`<a class="route" href="${esc(r.url)}">🗺️ ${esc(r.label)}</a>`).join('')}</div>${flow}</section>`}async function loadMedia(el){const title=el.dataset.wiki;if(!title)return;try{const r=await fetch('https://en.wikipedia.org/api/rest_v1/page/summary/'+encodeURIComponent(title));if(!r.ok)throw 0;const j=await r.json(),url=j.thumbnail?.source||j.originalimage?.source;if(!url)throw 0;const img=el.querySelector('img');img.src=url;img.onload=()=>el.classList.add('loaded');const a=el.querySelector('.credit');a.href=j.content_urls?.desktop?.page||('https://en.wikipedia.org/wiki/'+encodeURIComponent(title));}catch{}}function init(){document.getElementById('daynav').innerHTML=DAYS.map(d=>`<a href="#${d.id}"><b>${esc(d.short)}</b><small>${esc(d.date.split(' ',1)[0])} ${esc(d.date.split(' ').slice(1).join(' '))}</small></a>`).join('')+'<a href="#install"><b>📲</b><small>Installeren</small></a>';document.getElementById('app').innerHTML=DAYS.map(day).join('');const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){loadMedia(e.target);io.unobserve(e.target)}}),{rootMargin:'350px'});document.querySelectorAll('.media[data-wiki]').forEach(x=>io.observe(x));if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js')}document.addEventListener('DOMContentLoaded',init);